import { EjecucionServicio } from './ejecucion-servicio.model';

describe('EjecucionServicio', () => {
  it('should create an instance', () => {
    expect(new EjecucionServicio()).toBeTruthy();
  });
});
